const assert = require('assert'); 
const calculator = require('../src/calculator'); 
describe('Simple Math Test', () => { 
    it('should return 2', () => { 
        assert.equal(1 + 1, 2);
     });
});